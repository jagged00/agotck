
agot_tributary_setup_effect = {
	### Beyond the Wall
	if = {
		limit = {
			game_start_date > 7940.1.1
			game_start_date < 8297.1.1
		}
		title:k_thenn.holder = { # Magnar of the Thenns
			save_scope_as = suzerain
			title:c_chained_maw.holder = { # Vardok Clan
				break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			}
		}
	}
	if = {
		limit = {
			game_start_date > 8279.1.1
			game_start_date < 8285.1.1
		}
		title:d_wintermouth.holder = { # Rattleshirt
			save_scope_as = suzerain
			title:c_frost_caves.holder = { # Covehold Clan
				break_subject_contract_and_establish_tributary_effect = { SUZERAIN = scope:suzerain TRIBUTARY = this }
			}
		}
	}
	### Future places
}