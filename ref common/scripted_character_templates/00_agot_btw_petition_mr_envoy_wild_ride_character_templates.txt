AGOT_peasant_leader_martial_character = {
	age = { 20 40 }
	random_traits = yes
	culture = scope:nw_petition_target.culture
	faith = scope:nw_petition_target.faith
	gender_female_chance = {
		if = {
			limit = { scope:nw_petition_target.faith = { has_doctrine = doctrine_gender_male_dominated } }
			add = 0
		}
		else_if = {
			limit = { scope:nw_petition_target.faith = { has_doctrine = doctrine_gender_female_dominated } }
			add = 100
		}
		else = {
			add = 50
		}
	}
	trait = peasant_leader
	martial = {
		min_template_decent_skill
		max_template_decent_skill
	}
}