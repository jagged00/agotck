# Sets the custom duel wide event window to appear
agot_event_window_wide = {
	scope = character
	is_shown = {
		has_variable = agot_event_window_wide #AGOT ERROR - Used but not set
	}
}

agot_event_window_wide_false = {
	scope = character
	is_shown = {
		NOT = {
			has_variable = agot_event_window_wide  #AGOT ERROR - Used but not set
		}
	}
}

objects_equal = {
	is_shown = {
		root = scope:object
	}
}