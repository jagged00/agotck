namespace = agot_combat_event

#Increase
agot_combat_event.1000 = {
	hidden = yes
	scope = combat_side

	trigger = {
		exists = side_commander
		side_commander = { is_alive = yes }
		side_commander.faith = { has_doctrine_parameter = increased_dread_on_combat_win }
	}

	immediate = {
		side_commander = { add_dread = 5 }
	}
}
