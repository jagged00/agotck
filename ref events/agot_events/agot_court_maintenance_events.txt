namespace = agot_court_maintenance

#Fucking race conditions
agot_court_maintenance.1000 = {
	hidden = yes

	immediate = {
		if = {
			limit = {
				exists = scope:old_employer
			}
			scope:old_employer = {
				add_courtier ?= prev
			}
			set_location_to_default = yes
		}
		else_if = {
			limit = { exists = var:old_location }
			move_to_pool_at = var:old_location
		}
		else = {
			move_to_pool = yes
		}
	}
}